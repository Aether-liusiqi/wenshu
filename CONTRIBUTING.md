# 贡献指南

感谢你对 文书（Wenshu）的关注！无论是修正格式错误、补充范文、还是优化 Prompt，这份指南将帮助你快速上手。

## 环境搭建

本项目为纯 Markdown 项目，无外部依赖。只需：

- 一个 Markdown 编辑器（VSCode、Obsidian、Typora 等）
- 熟悉《党政机关公文处理工作条例》（2012）和 GB/T 9704-2012 格式标准
- Git（用于提交 PR）

```bash
# 克隆仓库
git clone https://github.com/Aether-liusiqi/wenshu.git
cd wenshu
```

## 可以贡献什么

| 贡献类型 | 说明 |
|---------|------|
| **格式修正** | 范文中的格式错误、规范引用不准确、字体字号标注偏差 |
| **范文补充** | 新的公文范文（特别是变体场景：紧急通知、联合行文、批转通知等） |
| **知识库扩展** | references/ 中的新深度文件（如"特定行业公文惯例""公文翻译注意事项"） |
| **Prompt 优化** | SKILL.md 中的角色定义、工作流、约束条件的改进 |
| **平台适配** | 在特定平台（Claude Code / Codex / OpenClaw / OpenCode）上发现的兼容性问题 |
| **文档翻译** | 将核心文件翻译为英文（非必需但欢迎） |

## 范文贡献规范

提交新的范文时，请确保：

1. **格式正确**。范文本身应严格符合 GB/T 9704-2012。格式提示中标注关键规范点。
2. **可替换性**。范文中的单位名称、人名、地名、日期、数据使用占位符命名（如"XX市""XX局""张三"），并在格式提示中说明。
3. **文种标识**。文件开头标注文种、行文方向和适用场景。
4. **文件命名**。遵循现有命名模式：`{文种}-{场景简述}.md`（如 `通知-年度会议通知.md`）。
5. **使用规范汉字**。避免口语化表达，不用网络用语。
6. **一文一事**。每篇范文只展示一个核心场景，不混合多种场景。

### 范文模板

```markdown
# 范文：{标题}

**文种**：{文种名称}（{行文方向}）
**适用场景**：{简述}
**格式要点**：{关键格式提示}

---

{完整公文正文}

---

## 格式提示

1. {提示一}
2. {提示二}
```

## 知识库贡献规范

references/ 中的文件请保持：

- 每个文件聚焦一个主题，控制在 500-1500 字
- 使用表格呈现对比性信息（如文种辨析）
- 引用《条例》或 GB/T 9704-2012 的具体条款时注明出处
- 格式错误用"✗ 错误写法 → ✓ 正确写法"模式呈现

## Pull Request 流程

1. Fork 仓库，创建 feature 分支。
2. 确保你的修改符合上述规范。
3. 在 PR 描述中说明修改了什么、为什么修改、依据是什么。
4. 打开 PR 到 `main` 分支。

## 许可

贡献即表示你同意将你的贡献以 MIT 许可包含在本项目中。

Copyright (c) 2026 Siqi Liu
